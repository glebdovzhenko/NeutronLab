Установка (Ubuntu / Debian):

# Python:
установка bzip2:
sudo apt-get update && sudo apt-get upgrade
sudo apt-get install bzip2

установка Python:
cd ~
wget https://repo.anaconda.com/archive/Anaconda3-5.2.0-Linux-x86_64.sh --no-check-certificate
chmod +x Anaconda3-5.2.0-Linux-x86_64.sh
./Anaconda3-5.2.0-Linux-x86_64.sh

# McStas
cd /etc/apt/sources.list.d
sudo wget http://packages.mccode.org/debian/mccode.list
sudo apt-get update
sudo apt-get install mcstas-2.4.1 mcstas-tools-python-mcrun-2.4.1 mcstas-tools-python-mccodelib-2.4.1
sudo apt-get install mpich

# NeutronLab
sudo apt-get upgrade
sudo apt-get install git
cd ~
git clone -b master https://github.com/glebdovzhenko/NeutronLab.git

# Дополнительные библиотеки
~/anaconda3/bin/pip install --upgrade pip
~/anaconda3/bin/pip install -r NeutronLab/requirements.txt --upgrade

# Запуск
cd NeutronLab
export PYTHONPATH=$(pwd)':'${PYTHONPATH}
cd executables
~/anaconda3/bin/python h1_sans.py

Установка (Windows 10 x64):

# Подготовка системы:
Следовать инструкциям с https://docs.microsoft.com/en-us/windows/wsl/install-win10
Установить X-Ming x-сервер: https://sourceforge.net/projects/xming/
ВНУТРИ приложения Debian под Windows исполнить команду
export DISPLAY=localhost:0.0

# Установка:
ВНУТРИ приложения Debian под Windows следовать инструкциям по установке на Ububtu/ Debian



Перед первым запуском:
необходимо поместить файл с ключом secret.key получаемым отдельно в папку NeutronLab и запустить скрипт unlock.py,
расшифровывающий данные об инструментах. Без этого программу запустить невозможно.